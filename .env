# Endereço interno do Eureka (nome do serviço na rede do Compose)
EUREKA_URL=http://discovery:8761/eureka

# Endereço interno do Config Server (nome do serviço na rede do Compose)
CONFIG_SERVER_URL=http://config-server:8888

# Perfil ativo nos containers (ajusta para 'docker' / 'prod' conforme o cenário)
SPRING_PROFILES_ACTIVE=docker

# Porta pública do Gateway no host
GATEWAY_PORT=8080

# Porta publicada pela 1.ª instância do tasks-service (réplicas adicionais ficam internas)
TASKS_PORT=8081
